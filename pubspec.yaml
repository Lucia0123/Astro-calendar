name: calendar
description: "Astro-calendar"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  riverpod: ^3.2.0
  flutter_riverpod: ^3.2.0
  moon_phase: ^1.1.3
  apsl_sun_calc: ^0.0.4
  http: ^1.6.0
  intl: ^0.20.2
  localstorage: ^6.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

